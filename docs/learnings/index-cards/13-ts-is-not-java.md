# Learning Card 13: TS Is Not Java

TypeScript helps at compile time.

It does not:

- enforce runtime types
- prevent `undefined` at runtime
- make JS synchronous

Watch for:

- looser typing
- optional values
- async everywhere
- package/tooling variability

Rule:

Use TS for guidance, not as a guarantee of runtime safety.
