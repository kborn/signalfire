name: ci

on:
  push:
  pull_request:

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        run: |
          echo "Configure CI once the stack is chosen."
          echo "Suggested: backend build + frontend build + tests + docs checks."
